config {
    dependencies: ["corp_grupo_sku_similar_cev"],
    tags: ["sqlserver", "corp"]
}

MERGE
  `TRUSTED.corp_grupo_sku_similar_cev` T
USING
  (
  WITH
    TABELA AS (
    SELECT
      DISTINCT CAST(COD_GRUPO_SKU_SIMILAR AS INT64) AS COD_GRUPO_SKU_SIMILAR,
      NOM_GRUPO_SKU_SIMILAR,
      CAST(COD_GRUPO_PRODUTO_SIMILAR AS INT64) AS COD_GRUPO_PRODUTO_SIMILAR,
      CAST(IND_ATIVO AS BOOL) AS IND_ATIVO
    FROM
      `RAW.corp_grupo_sku_similar_cev` A
    WHERE
      PARTITIONTIME=(
      SELECT
        MAX(PARTITIONTIME)
      FROM
        `RAW.corp_grupo_sku_similar_cev`))
  SELECT
    *
  FROM
    TABELA) S
ON
  T.COD_GRUPO_SKU_SIMILAR = S.COD_GRUPO_SKU_SIMILAR
  WHEN MATCHED THEN UPDATE SET T.COD_GRUPO_SKU_SIMILAR = S.COD_GRUPO_SKU_SIMILAR, T.NOM_GRUPO_SKU_SIMILAR = S.NOM_GRUPO_SKU_SIMILAR, T.COD_GRUPO_PRODUTO_SIMILAR = S.COD_GRUPO_PRODUTO_SIMILAR, T.IND_ATIVO = S.IND_ATIVO
  WHEN NOT MATCHED
  THEN
INSERT
  ( COD_GRUPO_SKU_SIMILAR,
    NOM_GRUPO_SKU_SIMILAR,
    COD_GRUPO_PRODUTO_SIMILAR,
    IND_ATIVO )
VALUES
  ( S.COD_GRUPO_SKU_SIMILAR, S.NOM_GRUPO_SKU_SIMILAR, S.COD_GRUPO_PRODUTO_SIMILAR, S.IND_ATIVO )
  WHEN NOT MATCHED BY SOURCE
  THEN
DELETE
  ;
