config {
    type: "operations",
    hasOutput: true,
    tags: ["sqlserver"]
}

INSERT INTO
  `RAW.cev_corp_tipo_classe_produto` ( 
	COD_CLASSE,
	NOM_CLASSE,
    PARTITIONTIME )
WITH
  TABELA AS (
  SELECT
	COD_CLASSE,
	NOM_CLASSE,
    _PARTITIONTIME AS PARTITIONTIME
  FROM
    `LAND.cev_corp_tipo_classe_produto` ),
  TABELA_B AS(
  SELECT
    MAX(PARTITIONTIME) AS PARTITIONTIME
  FROM
    `RAW.cev_corp_tipo_classe_produto` )
SELECT
  a.*
FROM
  TABELA a
WHERE
  a.PARTITIONTIME = (
  SELECT
    MAX(PARTITIONTIME)
  FROM
    TABELA)
