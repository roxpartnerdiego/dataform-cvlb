config {
    type: "operations",
    hasOutput: true,
    tags: ["sqlserver"]
}

INSERT INTO
  `RAW.corp_funcao_substituta_cev` ( 
	COD_FUNCAO_SUBSTITUTA,
    COD_FUNCAO,
    PARTITIONTIME )
WITH
  TABELA AS (
  SELECT
	COD_FUNCAO_SUBSTITUTA,
    COD_FUNCAO,
    _PARTITIONTIME AS PARTITIONTIME
  FROM
    `LAND.corp_funcao_substituta_cev` ),
  TABELA_B AS(
  SELECT
    MAX(PARTITIONTIME) AS PARTITIONTIME
  FROM
    `RAW.corp_funcao_substituta_cev` )
SELECT
  a.*
FROM
  TABELA a
WHERE
  a.PARTITIONTIME = (
  SELECT
    MAX(PARTITIONTIME)
  FROM
    TABELA)
