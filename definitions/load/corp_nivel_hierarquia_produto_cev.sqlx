config {
    type: "operations",
    hasOutput: true,
    tags: ["sqlserver"]
}

INSERT INTO
  `RAW.corp_nivel_hierarquia_produto_cev` ( 
	NUM_NIVEL_HIERARQUIA,
	NOM_NIVEL_HIERARQUIA,
    PARTITIONTIME )
WITH
  TABELA AS (
  SELECT
	NUM_NIVEL_HIERARQUIA,
	NOM_NIVEL_HIERARQUIA,
    _PARTITIONTIME AS PARTITIONTIME
  FROM
    `LAND.corp_nivel_hierarquia_produto_cev` ),
  TABELA_B AS(
  SELECT
    MAX(PARTITIONTIME) AS PARTITIONTIME
  FROM
    `RAW.corp_nivel_hierarquia_produto_cev` )
SELECT
  a.*
FROM
  TABELA a
WHERE
  a.PARTITIONTIME = (
  SELECT
    MAX(PARTITIONTIME)
  FROM
    TABELA)
