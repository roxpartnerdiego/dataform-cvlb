config {
    type: "operations",
    hasOutput: true,
    tags: ["sqlserver"]
}

INSERT INTO
  `RAW.corp_tipo_funcao_usuario_cev` ( 
	COD_TIPO_FUNCAO,
	NOM_TIPO_FUNCAO,
	NUM_COR_FUNDO,
	COD_SIGLA_TIPO,
	NUM_COR_FONTE,
	IND_BLOQUEIO_MESMA_HIERARQUIA,
    PARTITIONTIME )
WITH
  TABELA AS (
  SELECT
	COD_TIPO_FUNCAO,
	NOM_TIPO_FUNCAO,
	NUM_COR_FUNDO,
	COD_SIGLA_TIPO,
	NUM_COR_FONTE,
	IND_BLOQUEIO_MESMA_HIERARQUIA,
    _PARTITIONTIME AS PARTITIONTIME
  FROM
    `LAND.corp_tipo_funcao_usuario_cev` ),
  TABELA_B AS(
  SELECT
    MAX(PARTITIONTIME) AS PARTITIONTIME
  FROM
    `RAW.corp_tipo_funcao_usuario_cev` )
SELECT
  a.*
FROM
  TABELA a
WHERE
  a.PARTITIONTIME = (
  SELECT
    MAX(PARTITIONTIME)
  FROM
    TABELA)
