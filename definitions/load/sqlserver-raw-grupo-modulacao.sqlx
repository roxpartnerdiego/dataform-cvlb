config {
    type: "operations",
    hasOutput: true,
    tags: ["sqlserver"]
}

INSERT INTO
  `RAW.cev_corp_grupo_modulacao` ( 
	COD_GRUPMODUL,
	NOM_GRUPMODUL,
	COD_USU_INC,
	COD_USU_ALT,
	DAT_INC,
	DAT_ALT,
	DAT_EXPORTACAO,
	IND_OPER_EXPORTACAO,
	IND_CORTE_ONDA,
    PARTITIONTIME )
WITH
  TABELA AS (
  SELECT
	COD_GRUPMODUL,
	NOM_GRUPMODUL,
	COD_USU_INC,
	COD_USU_ALT,
	DAT_INC,
	DAT_ALT,
	DAT_EXPORTACAO,
	IND_OPER_EXPORTACAO,
	IND_CORTE_ONDA,
    _PARTITIONTIME AS PARTITIONTIME
  FROM
    `LAND.cev_corp_grupo_modulacao` ),
  TABELA_B AS(
  SELECT
    MAX(PARTITIONTIME) AS PARTITIONTIME
  FROM
    `RAW.cev_corp_grupo_modulacao` )
SELECT
  a.*
FROM
  TABELA a
WHERE
  a.PARTITIONTIME = (
  SELECT
    MAX(PARTITIONTIME)
  FROM
    TABELA)
