config {
    type: "operations",
    hasOutput: true,
    tags: ["sqlserver"]
}

INSERT INTO
  `RAW.cic_hierarquia_unidade_de_negocio_cev` (
     COD_HIERARQUIA_PRODUTO,
    UN_INGLES,
    UN_PORTUGUES,
    UN_DESCRICAO,
    PARTITIONTIME )
WITH
  TABELA AS (
  SELECT
     COD_HIERARQUIA_PRODUTO,
    UN_INGLES,
    UN_PORTUGUES,
    UN_DESCRICAO,
     _PARTITIONTIME AS PARTITIONTIME
  FROM
    `LAND.cic_hierarquia_unidade_de_negocio_cev` ),
  TABELA_B AS(
  SELECT
    MAX(PARTITIONTIME) AS PARTITIONTIME
  FROM
    `RAW.cic_hierarquia_unidade_de_negocio_cev` )
SELECT
  a.*
FROM
  TABELA a
WHERE
  a.PARTITIONTIME = (
  SELECT
    MAX(PARTITIONTIME)
  FROM
    TABELA)
