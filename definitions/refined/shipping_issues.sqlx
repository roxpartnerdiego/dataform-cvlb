config {
    tags: ["cubo-vendas", "oms"]
}

TRUNCATE TABLE REFINED.SHIPPING_ISSUES;

INSERT INTO
    REFINED.SHIPPING_ISSUES (
SELECT *, "CEV" AS BANDEIRA, CURRENT_TIMESTAMP() AS DATA_ATUALIZACAO
FROM `TRUSTED.oms_shipping_issues_cev` )